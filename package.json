{
  "name": "modelrelay-cloudflare-zero-cost",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-flags=nodejs_compat",
    "deploy": "wrangler pages deploy public --project-name modelrelay --compatibility-flags=nodejs_compat"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@aptos-labs/ts-sdk": "^7.2.0",
    "@shelby-protocol/sdk": "^0.3.1"
  }
}
