{
  "name": "provenode",
  "version": "2.0.0",
  "private": true,
  "description": "Zero-cost edge AI model deployment on Shelby testnet — Vercel edition",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step required'",
    "deploy": "vercel --prod",
    "lint": "eslint 'api/**/*.js'"
  },
  "dependencies": {
    "@aptos-labs/ts-sdk": "^6.0.0",
    "@shelby-protocol/sdk": "^0.3.1",
    "@vercel/kv": "^3.0.0",
    "formidable": "^3.5.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "vercel": "^35.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "@aptos-labs/ts-sdk": "^6.0.0"
  }
}
